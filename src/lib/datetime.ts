/**
 * Timestamps are stored in UTC (the MySQL pool runs with `timezone: "Z"`) and
 * displayed in Paraguay's civil time. Dates render as dd/mm/yyyy.
 */
export const ASUNCION_TIME_ZONE = "America/Asuncion";
