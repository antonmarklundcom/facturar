# Decision 12: the local gate replaces GitHub Actions CI.
# Everything that would have run in CI runs here, and green must be real.
set -e

echo "pre-push: lint"
npm run lint

echo "pre-push: typecheck"
npm run typecheck

echo "pre-push: test"
npm run test

echo "pre-push: build"
npm run build
